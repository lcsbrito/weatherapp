import { useState } from "react"
import { XStack, YStack, Button, Input, Card } from "tamagui"

export default function IndexScreen() {
return(
  <XStack fullscreen flex={1} backgroundColor={"$black025"}>
  </XStack>
)
}